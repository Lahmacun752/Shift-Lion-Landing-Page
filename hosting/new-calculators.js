(()=>{const q=id=>document.getElementById(id),n=id=>Number(q(id)?.value||0),money=value=>new Intl.NumberFormat(document.documentElement.lang,{style:'currency',currency:'EUR'}).format(value),hours=value=>`${value.toLocaleString(document.documentElement.lang,{maximumFractionDigits:2})} h`,kind=document.body.dataset.calculator,en=document.documentElement.lang==='en';
const calculate=()=>{let formula='';if(kind==='monthly-pay'){const wage=n('wage'),early=n('early'),late=n('late'),night=n('night'),sunday=n('sunday'),holiday=n('holiday'),totalHours=early+late+night+sunday+holiday,base=totalHours*wage,allowances=late*wage*n('lateRate')/100+night*wage*n('nightRate')/100+sunday*wage*n('sundayRate')/100+holiday*wage*n('holidayRate')/100;q('hoursResult').textContent=hours(totalHours);q('baseResult').textContent=money(base);q('allowanceResult').textContent=money(allowances);q('totalResult').textContent=money(base+allowances);formula=en?'Base pay = all entered hours × hourly wage. Each allowance uses its own hours and rate.':'Grundlohn = alle eingetragenen Stunden × Stundenlohn. Jeder Zuschlag nutzt die zugehörigen Stunden und den eigenen Satz.'}
if(kind==='break'){const work=n('hours'),minutes=work>9?45:work>6?30:0;q('breakResult').textContent=`${minutes} min`;q('splitResult').textContent=minutes?`${minutes/15} × 15 min`:(en?'No statutory minimum from duration alone':'Keine Mindestpause allein aus der Dauer');q('netResult').textContent=hours(work+minutes/60);formula=en?'German general rule: over 6 up to 9 hours requires at least 30 minutes; over 9 hours requires 45 minutes.':'Allgemeine Regel: Bei mehr als 6 bis 9 Stunden mindestens 30 Minuten, bei mehr als 9 Stunden 45 Minuten.'}
if(kind==='sunday'){const base=n('hours')*n('wage'),extra=base*n('rate')/100;q('baseResult').textContent=money(base);q('allowanceResult').textContent=money(extra);q('totalResult').textContent=money(base+extra);formula=en?'Sunday hours × hourly wage; allowance = base pay × entered rate. A general statutory entitlement to a Sunday premium does not automatically follow from the calculator.':'Sonntagsstunden × Stundenlohn; Zuschlag = Grundlohn × eingegebener Satz. Ein allgemeiner gesetzlicher Anspruch auf Sonntagszuschlag folgt daraus nicht automatisch.'}
if(kind==='rest'){const start=new Date(q('shiftEnd').value),end=new Date(q('nextStart').value),diff=(end-start)/36e5;if(!Number.isFinite(diff)||diff<0){q('newCalculatorError').textContent=en?'The next shift must start after the previous shift ends.':'Die nächste Schicht muss nach dem Ende der vorherigen beginnen.';return}q('newCalculatorError').textContent='';q('restResult').textContent=hours(diff);const delta=diff-11;q('differenceResult').textContent=`${delta>=0?'+':''}${delta.toLocaleString(document.documentElement.lang,{maximumFractionDigits:2})} h`;q('statusResult').textContent=diff>=11?(en?'General 11-hour period reached':'Allgemeine 11-Stunden-Ruhezeit erreicht'):(en?'Below the general 11-hour period':'Unter der allgemeinen 11-Stunden-Ruhezeit');formula=en?'Time from the end of the previous shift to the start of the next shift. Statutory exceptions are not assessed.':'Zeit vom Ende der vorherigen bis zum Beginn der nächsten Schicht. Gesetzliche Ausnahmen werden nicht geprüft.'}
if(kind==='leave'){const annual=n('fullLeave')/Math.max(n('referenceDays'),.01)*n('actualDays'),partial=annual*Math.min(Math.max(n('months'),0),12)/12;q('annualResult').textContent=`${annual.toLocaleString(document.documentElement.lang,{maximumFractionDigits:2})} ${en?'days':'Tage'}`;q('partialResult').textContent=`${partial.toLocaleString(document.documentElement.lang,{maximumFractionDigits:2})} ${en?'days':'Tage'}`;q('weeksResult').textContent=`${(annual/Math.max(n('actualDays'),.01)).toLocaleString(document.documentElement.lang,{maximumFractionDigits:2})} ${en?'weeks':'Wochen'}`;formula=en?'Reference annual leave ÷ reference workdays × your workdays. The monthly value is a simple proportional estimate.':'Jahresurlaub im Referenzmodell ÷ Referenz-Arbeitstage × deine Arbeitstage. Der Monatswert ist eine einfache anteilige Schätzung.'}q('formula').textContent=formula};q('calculate')?.addEventListener('click',calculate);document.querySelector('.calculator')?.addEventListener('input',()=>{if(kind!=='rest'||q('shiftEnd').value&&q('nextStart').value)calculate()});calculate()})();
