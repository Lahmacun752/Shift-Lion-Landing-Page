(()=>{const en=document.documentElement.lang==='en',days=document.querySelector('#days'),template=document.querySelector('#dayTemplate'),target=document.querySelector('#target'),breakDefault=document.querySelector('#breakDefault');let number=0;const minutes=value=>{if(!value)return null;const [h,m]=value.split(':').map(Number);return h*60+m};const format=value=>{const sign=value<0?'-':'';value=Math.abs(Math.round(value));return `${sign}${Math.floor(value/60)}:${String(value%60).padStart(2,'0')} h`};function calculate(){let gross=0,breaks=0,net=0,worked=0;days.querySelectorAll('.day-row').forEach(row=>{const start=minutes(row.querySelector('.start').value),endValue=minutes(row.querySelector('.end').value),pause=Math.max(0,Number(row.querySelector('.pause').value)||0),result=row.querySelector('.result');if(start===null||endValue===null){result.textContent='–';return}let end=endValue;if(end<start)end+=1440;const duration=Math.max(0,end-start),dailyNet=Math.max(0,duration-pause);gross+=duration;breaks+=Math.min(pause,duration);net+=dailyNet;worked++;result.textContent=format(dailyNet)});const expected=worked*(Math.max(0,Number(target.value)||0)*60);document.querySelector('#gross').textContent=format(gross);document.querySelector('#breaks').textContent=format(breaks);document.querySelector('#net').textContent=format(net);const balance=net-expected,balanceBox=document.querySelector('.balance');document.querySelector('#balance').textContent=(balance>0?'+':'')+format(balance);balanceBox.classList.toggle('positive',balance>=0);balanceBox.classList.toggle('negative',balance<0)}function addDay(){number++;const fragment=template.content.cloneNode(true),row=fragment.querySelector('.day-row');row.querySelector('.day-name').textContent=(en?'Day ':'Tag ')+number;row.querySelector('.pause').value=breakDefault.value;row.querySelector('.remove').addEventListener('click',()=>{row.remove();calculate()});row.querySelectorAll('input').forEach(input=>input.addEventListener('input',calculate));days.appendChild(row);calculate()}document.querySelector('#addDay').addEventListener('click',addDay);document.querySelector('#calculate').addEventListener('click',calculate);target.addEventListener('input',calculate);breakDefault.addEventListener('change',()=>{days.querySelectorAll('.pause').forEach(input=>{if(!input.value)input.value=breakDefault.value});calculate()});document.querySelector('.menu-toggle')?.addEventListener('click',event=>{const menu=document.querySelector('.mobile-menu'),open=event.currentTarget.getAttribute('aria-expanded')==='true';event.currentTarget.setAttribute('aria-expanded',String(!open));menu.hidden=open});addDay();addDay();addDay()})();
